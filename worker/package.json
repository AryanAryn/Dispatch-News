{
    "name": "dispatch-proxy",
    "version": "1.0.0",
    "private": true,
    "description": "Cloudflare Worker – CORS proxy for NewsAPI used by the Dispatch News app.",
    "scripts": {
        "dev":    "wrangler dev",
        "deploy": "wrangler deploy",
        "tail":   "wrangler tail"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}
